{
  "buildCommand": "npm cache clean --force && rm -rf node_modules package-lock.json && npm install --legacy-peer-deps && npm install -D tailwindcss postcss autoprefixer && npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "devCommand": "next dev"
}